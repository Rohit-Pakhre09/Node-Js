const areaOfCircle = (r) => {
    return 3.14 * r * r;
};

const areaOfSquare = (s) => {
    return s * s;
};

const areaOfRectangle = (l, b) => {
    return l * b;
};

module.exports = {
    areaOfCircle,
    areaOfSquare,
    areaOfRectangle
};